<parameters>
  <param label="Number of nodes" name="nodes" type="integer" min="1" max="4" value="1" help="Number of compute nodes to use, '--nodes' value (1-4). Leave blank to use default value." />
  <param label="Number of cores to use" name="processors" type="integer" min="1" max="64" value="1" help="Number of processing cores, '--ntasks' value (1-64). Leave blank to use default value." />
  <param label="Memory per core in Megabytes" name="memory" type="integer" min="512" max="262144" value="1024" help="Memory size in MB, '--mem' value (512-262144). Leave blank to use default value." />   -->
  <param label="Estimated run time in hours" name="time" type="integer" min="1" max="744" value="1" help="Maximum job time in hours, '--time' value (1-744). Leave blank to use default value." />

  <param label="Partition" name="partition" type="select" value="" help="Select the partition to use">
       <option value="normal">Normal</option>
       <option value="bigmem">Bigmem</option>
       <option value="optimist">Optimist</option>
  </param>
  <param label="Project" name="project" type="select" value="" help="Project to assign resource allocation to.">
       <option value="----">Select the project you want to run the job</option>
  </param>
  
  <param label="Saga home folder" name="userhome" type="select" value="" help="Project to assign resource allocation to.">
       <option value="----">Select the project you want to run the job</option>
  </param>
</parameters>
