<?xml version="1.0"?>
<toolbox>
    <!--
    This is Galaxy's integrated tool panel and should be modified directly only for
    reordering tools inside a section. Each time Galaxy starts up, this file is
    synchronized with the various tool config files: tools, sections and labels
    added to one of these files, will be added also here in the appropriate place,
    while elements removed from the tool config files will be correspondingly
    deleted from this file.
    To modify locally managed tools (e.g. from tool_conf.xml) modify that file
    directly and restart Galaxy. Whenever possible Tool Shed managed tools (e.g.
    from shed_tool_conf.xml) should be managed from within the Galaxy interface or
    via its API - but if changes are necessary (such as to hide a tool or re-assign
    its section) modify that file and restart Galaxy.
    -->
    <section id="test" name="Tests" version="">

        <tool id="module_test" />

    </section>

    <section id="getext" name="Get Data" version="">

        <tool id="upload" />

    </section>

    <label id="hpc" text="HPC tools" version="" />

    <section id="Assembly" name="BWA" version="">

        <tool id="spades" />

    </section>

    <section id="Mapping" name="BWA" version="">

        <tool id="bwa" />

    </section>

    <section id="Bowtie2" name="BWA" version="">

        <tool id="bowtie2" />

    </section>
    <section id="blast" name="NCBI BLAST+" version="">

        <tool id="ncbi_tblastn_wrapper" />

        <tool id="blastxml_to_tabular" />

        <tool id="ncbi_blastdbcmd_info" />

        <tool id="ncbi_blastdbcmd_wrapper" />

        <tool id="ncbi_blastn_wrapper" />

        <tool id="ncbi_blastp_wrapper" />

        <tool id="ncbi_blastx_wrapper" />

        <tool id="ncbi_convert2blastmask_wrapper" />

        <tool id="ncbi_dustmasker_wrapper" />

        <tool id="ncbi_makeblastdb" />

        <tool id="ncbi_makeprofiledb" />

        <tool id="ncbi_rpsblast_wrapper" />

        <tool id="ncbi_rpstblastn_wrapper" />

        <tool id="ncbi_segmasker_wrapper" />

        <tool id="ncbi_tblastx_wrapper" />

    </section>

    <section id="RNAseq" name="RNA-seq" version="">

        <tool id="trinity" />

    </section>
</toolbox>
